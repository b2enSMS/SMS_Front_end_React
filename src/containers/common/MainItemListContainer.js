import React from 'react';
import { MainItemList } from "components"

const MainItemListContainer = () => {
    return (
        <MainItemList
        />
    );
};

export default MainItemListContainer;